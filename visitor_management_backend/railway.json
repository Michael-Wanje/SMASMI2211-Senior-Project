{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn visitor_management_backend.wsgi:application --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/admin/"
  }
}